docker service create -d --network overmind --name engineering-bay --replicas 2 -e OC_USERNAME=root -e OC_PASSWD=root 42school/engineering-bay

#A replicated service is a Docker Swarm service that has a specified number
#of replicas running. These replicas consist of multiple instances of 
#the specified Docker container.

