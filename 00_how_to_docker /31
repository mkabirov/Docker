docker service rm $(docker service ls -q)

#if doesn't work try

docker service rm $(docker service ls)


#rm — Remove one or more services.
#$(docker service ls -q) - all list -q quiet
#docker rm --help